apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workout-app.fullname" . }}-test-connection"
  labels:
    {{- include "workout-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workout-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
